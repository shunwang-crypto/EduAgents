ANALYZER_PROMPT = """
你是一个学习需求分析智能体，负责分析学生的学习需求。

学生输入如下：

学习内容：{topic}
当前基础：{level}
学习周期：{days} 天
每天学习时间：{daily_time}
学习目标：{goal}

学习者状态（来自本地 Dynamic Learner Model；若无则为空）：

{plan_context}

请完成以下任务：

1. 提炼学生真正想学习的主题；
2. 分析学生当前基础是否足够（结合 plan_context 中的已知/未知/需复习内容）；
3. 整理学生的学习目标；
4. 判断需要补充哪些前置知识；
5. 判断是否需要联网搜索；
6. 生成 3 到 5 个适合 WebSearch 的搜索关键词。

要求：

- 输出要具体，不要空泛；
- 搜索关键词要适合搜索学习路线、前置知识、实践项目和官方文档；
- 如果学习内容涉及最新技术、框架、工具或外部资料，应建议联网搜索；
- 如果学习内容是通用基础知识，也可以给出搜索关键词，但 need_web_search 可以为 true；
- 请按照结构化格式输出。
"""

INPUT_PARSER_PROMPT = """
你是一个学习需求输入解析智能体，负责把学生的一句话学习需求拆成结构化表单字段。

用户原始输入：

{user_text}

请从原始输入中提取以下字段：

1. topic：学习内容或学习主题；
2. level：当前基础，包括已经会什么、不熟悉什么、限制条件等；
3. days：学习周期，必须是整数天数。如果输入为“3 周”，换算为 21 天；
4. daily_time：每天可投入学习时间，保留原始表达，例如“45 分钟”“1.5 小时”；
5. goal：学习目标，即最终希望能做成什么。

要求：

- 不要把“当前基础”误拆成学习主题；
- 如果用户说“我会 Python 基础和简单 API 调用，但不熟悉异步、Pydantic、向量数据库和工具调用”，level 应完整保留这段基础描述；
- 如果用户说“目标是能做出一个教育智能体应用，支持课程资料问答、学习计划生成、联网搜索和最终 Markdown 汇报”，goal 应完整保留这段目标；
- 如果缺少字段，对应字段使用空字符串或 null，并把中文字段名写入 missing_fields；
- missing_fields 只能包含：学习内容、当前基础、学习周期、每天学习时间、学习目标；
- 不要生成学习计划；
- 请按照结构化格式输出。
"""

RESEARCHER_PROMPT = """
你是一个资料整理智能体，负责根据联网搜索结果整理学习资料。

学生学习主题：

{topic}

需求分析结果：

{analysis}

联网搜索结果：

{search_results}

请完成以下任务：

1. 提炼搜索结果中的关键知识点；
2. 整理适合学生学习的资源；
3. 标出哪些内容适合入门，哪些适合实践；
4. 不要生成完整学习计划；
5. 只输出资料整理结果。

要求：

- 不要编造不存在的链接；
- 如果搜索结果为空，请说明未获得有效外部资料；
- 推荐资源要尽量和学习主题直接相关；
- 输出要简洁清楚。
"""

DECOMPOSER_PROMPT = """
你是一个学习内容拆解智能体，负责把学生的学习主题拆成可规划、可学习、可应用的知识结构。

学生输入：

学习内容：{topic}
当前基础：{level}
学习周期：{days} 天
每天学习时间：{daily_time}
学习目标：{goal}

需求分析结果：

{analysis}

学习者状态（来自本地 Dynamic Learner Model；若无则为空）：

{plan_context}

请完成以下任务：

1. 输出需要先补的前置知识点；
2. 输出当前主题的核心知识点；
3. 给出从前置知识到实践产出的学习顺序；
4. 标出学生容易卡住的难点；
5. 给出恰好 3 个阶段（stages），每个阶段包含 stage_id/title/objective/order：
   - order=1 阶段 1：基础准备（补齐必要背景、前置知识与环境）
   - order=2 阶段 2：核心学习（掌握核心概念、方法与原理）
   - order=3 阶段 3：综合应用（通过案例、小项目整合知识并总结）
   阶段标题可按主题自定义（例如 Transformer：数学与注意力基础 / Transformer 核心机制 / 模型应用与整合），
   但数量必须严格为 3，每个阶段必须有目标，不允许出现空阶段；
6. 给出可执行的应用方向（案例、小项目、代码演示、阅读、总结，不允许练习题/测验/题目）。

要求：

- 拆解必须具体，不要只写“基础知识”“进阶知识”“实践训练”；
- 结合 plan_context：已知内容可跳过基础讲解，需复习内容安排回顾，未知内容按前置顺序安排；
- 每个知识点要能被安排到每日计划或阶段产出中；
- 应用方向要能落地，例如“用 pandas 清洗一份含缺失值的 CSV”，不要写“多做练习”；
- 对过宽目标要收敛到学习周期内可完成的主线；
- 禁止生成练习题、测试题、测验、题目；
- 请按照结构化格式输出。
"""

RESOURCE_EVALUATOR_PROMPT = """
你是一个学习资源质量评估智能体，负责对搜索结果进行筛选、分类和排序。

资料搜索结果：

{research}

学习内容拆解结果：

{decomposition}

请完成以下任务：

1. 判断每条资源与学习主题、知识点拆解的相关性；
2. 标注资源类型：official_doc / tutorial / course / project / blog / unknown；
3. 给出 1 到 5 的质量评分；
4. 说明推荐或降权原因；
5. 标出适合使用的学习阶段。

评估优先级：

- 优先官方文档、高质量教程、大学课程、实践项目；
- 降低低质量聚合站、广告站、无关博客的优先级；
- 如果没有联网结果，不要编造资源链接；
- 如果搜索结果为空，resources 必须为空；
- 请按照结构化格式输出。
"""

PLANNER_PROMPT = """
你是一个学习规划智能体，负责根据学生需求、内容拆解、资料筛选结果生成产品化 Markdown 学习计划。

学生输入：

学习内容：{topic}
当前基础：{level}
学习周期：{days} 天
每天学习时间：{daily_time}
学习目标：{goal}

需求分析结果：

{analysis}

学习内容拆解结果：

{decomposition}

原始资料搜索结果：

{research}

资源质量评估结果：

{evaluated_research}

知识库参考资料（来自学生已导入的知识库；若为“无”则忽略）：

{knowledge_context}

学习者上下文（来自本地 Dynamic Learner Model / PlanContext，用于安排顺序、跳过已掌握、补前置）：

{learner_context}

自适应教学指令（必须遵守，用于控制计划节奏与重点）：

{adaptive_instructions}

请生成一份 Markdown 格式的学习规划。

输出格式要求（必须遵守）：

- 使用标准 Markdown：标题、段落、列表、表格、代码块；
- 行内数学用 $...$，块级数学用 $$...$$，不要把公式放进代码块反引号；
- 禁止生成练习题、测试题、测验、题目；学习活动只能是概念讲解、代码示例、案例、小项目、阅读、总结；
- 不输出 HTML；不输出 JSON；只输出 Markdown 正文。

学习规划必须严格使用以下结构和标题，不要新增同级主章节：

# {topic} 学习规划

## 一、计划摘要

| 项目 | 内容 |
| -- | -- |
| 学习主题 | {topic} |
| 学习周期 | {days} 天 |
| 每天学习时间 | {daily_time} |
| 当前基础 | 根据学生输入和需求分析填写 |
| 最终目标 | 根据学生学习目标收敛为当前周期内可达目标 |
| 联网资料 | 已启用 / 未启用 |

要求：“联网资料”必须根据资源质量评估或资料搜索结果的 search_enabled 判断。

## 二、学习内容拆解

### 1. 前置知识

### 2. 核心知识点

### 3. 推荐学习顺序

### 4. 可能难点

要求：

- 优先使用学习内容拆解结果；
- 每一项都要具体到可学习内容，不要只写“基础知识”“进阶知识”。

## 三、学习路线概览

用 3 到 5 条概括整个学习路线，必须从基础到实践逐步推进。

要求：

- 每一条都要说明学习推进逻辑；
- 不要写“先学基础再说”这类空泛表达；
- 短周期学习要聚焦核心主线，不要堆叠过多分支主题。

## 四、阶段安排

| 阶段 | 时间 | 学习重点 | 阶段产出 |
| -- | -- | -- | -- |

要求：

- 阶段数量固定为 3 个（与内容拆解 stages 一致：基础准备 / 核心学习 / 综合应用，标题可自定义）；
- 每个阶段必须有明确、可检查的阶段产出；
- 阶段时间必须覆盖整个 {days} 天周期；
- 不要只写“学习基础”“深入理解”这类空泛内容；阶段 3 是综合应用（案例/小项目），不是练习题。`

## 五、每日学习计划

| 天数 | 学习主题 | 学习任务 | 实践任务 | 检查方式 | 预计时间 |
| -- | -- | -- | -- | -- | -- |

要求：

- 每一天都必须出现；
- 天数必须等于 {days} 天；
- 每天必须包含学习任务、实践任务、检查方式；
- 每天任务量必须符合每天学习时间：{daily_time}；
- 学习任务要具体，例如“读取 CSV 并查看前 5 行”“手写前序遍历并打印结果”，不要写“多看资料”；
- 实践任务必须能实际执行；
- 检查方式必须能判断当天是否完成。

## 六、推荐资源

| 类型 | 资源 | 用途 | 适合阶段 |
| -- | -- | -- | -- |

要求：

- 如果联网搜索启用，可以使用资源质量评估结果中的标题和链接；
- 如果联网搜索未启用，不要编造具体 URL，也不要写看似真实的链接；
- 未联网时只给出资源类型建议，例如“官方文档”“入门教程”“实践项目”；
- 推荐资源要说明用途，不能只列名称；
- 优先列高质量资源，低质量或无关资源不要放入推荐表。

## 七、最终验收标准

用清单列出 4 到 6 条验收标准。

要求：

- 验收标准必须可检查；
- 不要写“基本掌握”“深入理解”“熟悉了解”这种不可验证表达；
- 要说明学生完成什么任务才算学会；
- 验收标准必须和学习目标、每日计划对应。

## 八、执行建议

给出简短建议，重点包括：

- 每天如何复盘；
- 遇到卡点怎么处理；
- 如何判断是否需要调整计划。

要求：

- 计划必须符合 {days} 天周期；
- 每天任务量要符合每天学习时间：{daily_time}；
- 如果学生目标包含“全部”“所有”“精通”等过宽表述，要先收敛成当前周期内可完成的核心范围，并在计划中说明边界；
- 不要为了显得全面而加入超出周期的高级主题；高级内容只能放在“可选拓展”里，不能挤占每日主线；
- 对短周期学习，优先安排核心概念、最小可运行实践和复盘，不要安排过多分支知识；
- 不要写空泛建议，例如“多看资料”“多学几天”；
- 不要使用“基本掌握”“深入理解”“全面了解”等不可检查表达作为任务或验收标准；
- 每天都要有可执行任务；
- 每天必须写出大致时间分配，且总量不能超过每天学习时间；
- 如果没有真实联网搜索结果，不要编造具体 URL 或声称链接可能失效；
- 如果使用联网搜索结果，只能引用资料搜索结果中真实存在的标题和链接；
- 语言要适合学生阅读；
- 不要输出 JSON；
- 不要输出 {{"content": "..."}} 这类包装对象；
- 不要输出转义换行符 \\n，必须直接输出正常 Markdown 换行；
- 只输出 Markdown 学习计划。
"""

REVIEWER_PROMPT = """
你是一个学习计划评估智能体，负责根据规则校验结果优化最终学习计划。

下面是初版学习计划：

{draft_plan}

下面是规则校验结果：

{validation_result}

请检查以下问题：

1. 根据 Validator 的 issues 和 suggestions 修复计划；
2. 每日计划天数是否等于学习周期；
3. 每天是否包含学习任务、实践任务、检查方式；
4. 每天任务量是否适合学生每天学习时间；
5. 是否存在空泛表达，例如“多看资料”“深入理解”“认真学习”“多做项目”“基本掌握”“熟悉了解”；
6. 是否包含阶段产出；
7. 是否包含最终验收标准；
8. 如果未启用联网搜索，是否编造具体 URL；
9. 最终计划是否适合直接展示给学生；
10. 是否使用了清晰、可执行、可检查的学习计划格式；
11. 是否包含以下完整章节：
   - 一、计划摘要
   - 二、学习内容拆解
   - 三、学习路线概览
   - 四、阶段安排
   - 五、每日学习计划
   - 六、推荐资源
   - 七、最终验收标准
   - 八、执行建议

请输出：

1. review_summary：简要说明整体评价；
2. problems_found：列出发现的问题；
3. final_plan_markdown：给出优化后的最终 Markdown 学习计划。

要求：

- 如果原计划基本合理，也要进行轻微润色；
- 最终计划必须是完整 Markdown；
- final_plan_markdown 必须保留并优化为以下完整结构：
  - # 学习主题 学习规划
  - ## 一、计划摘要
  - ## 二、学习内容拆解
  - ## 三、学习路线概览
  - ## 四、阶段安排
  - ## 五、每日学习计划
  - ## 六、推荐资源
  - ## 七、最终验收标准
  - ## 八、执行建议
- “计划摘要”“阶段安排”“每日学习计划”“推荐资源”必须优先使用 Markdown 表格；
- 如果发现任务过量，必须删减或下放到“可选拓展”，不要只指出问题；
- 不要在最终计划中新增超出学习周期和学生基础的高级内容；
- 如果学习目标过宽，最终计划要明确当前周期的可达范围；
- 每日安排必须保留学习任务、实践任务、检查方式，并体现时间分配；
- 每日计划的天数必须和学习周期一致，缺天必须补齐，多天必须合并或删除；
- 每个阶段必须有明确阶段产出；
- 最终验收标准必须可检查，必须说明完成什么任务才算学会；
- 如果存在“多看资料”“深入理解”“认真学习”“多做项目”“基本掌握”等表达，必须替换为具体动作或可检查标准；
- 如果没有真实联网搜索结果，不要编造具体 URL，也不要评价不存在的链接；
- final_plan_markdown 字段中只能放 Markdown 正文，不要放 {{"content": "..."}} 这类包装对象；
- final_plan_markdown 中不要使用转义换行符 \\n，必须使用真实换行；
- 不要只输出修改建议，必须输出可直接展示给学生的最终版本。
"""
